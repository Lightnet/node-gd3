function gd3_call(){
    if(engine != null){
        console.log("found! var engine 3.0!");
        //console.log(engine.startGame);
        //console.log(game);
        
        console.log(window);
        console.log(engine);
        console.log(engine.startGame[0]);
    }else{

    }
}